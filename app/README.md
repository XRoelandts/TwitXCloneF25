Xavier Roelandts

The UI will be in the xml files of LoginActivity SignUpActivity and Message Activity and
the defined string resources is in the strings.xml

The Image resource is in the minimap and the color resource will be in the colors.xml

